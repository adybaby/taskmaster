import React from 'react';
import Typography from '@material-ui/core/Typography';

const ProfilePanel = () => (
  <div>
    <Typography>Profile TBD</Typography>
  </div>
);

export default ProfilePanel;
